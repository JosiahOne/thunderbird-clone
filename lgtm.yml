extraction:
  cpp:
    after_prepare:    # Customizable step used by all languages.
      - ./bootstrap.sh 
    index:    # Customizable step used by all languages.
      build_command:
        - ./build.sh
